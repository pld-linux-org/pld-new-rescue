
# the very base packages
# for the system (just so it boots) and the initramfs

basesystem
systemd-init
udev
kernel
busybox
losetup

# force new util-linux for blkid with ISO UUID support
util-linux 2.24

# this will be removed shortly after
localedb-src
